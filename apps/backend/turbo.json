{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "persistent": true,
      "with": ["demo-mcp-server#dev"],
      "dependsOn": ["^build"],
      "env": ["DEEPSEEK_API_KEY"]
    },
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    }
  }
}
