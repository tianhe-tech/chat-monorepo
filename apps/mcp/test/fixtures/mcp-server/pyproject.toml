[project]
name = "mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "wudi", email = "wicii2120@outlook.com" }]
requires-python = ">=3.12"
dependencies = ["fastmcp>=2.12.3", "ruff>=0.13.2"]

[project.scripts]
mcp-server = "mcp_server:main"

[build-system]
requires = ["uv_build>=0.8.22,<0.9.0"]
build-backend = "uv_build"

[tool.ruff]
line-length = 120

[tool.ruff.format]
quote-style = "single"
